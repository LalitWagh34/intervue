{
  "name": "@intervue/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "@intervue/db": "workspace:*",
    "better-auth": "^1.6.22",
    "dotenv": "^16.0.0",
    "groq-sdk": "^1.2.1",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}